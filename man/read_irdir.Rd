% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_irdir.R
\name{read_irdir}
\alias{read_irdir}
\title{Read and merge IRFinder outputs for control and experiment}
\usage{
read_irdir(
  control_dir,
  experiment_dir,
  control_name,
  experiment_name,
  splice_min = 10
)
}
\arguments{
\item{control_dir}{Path to directory with IRFinder outputs for control sample.}

\item{experiment_dir}{Path to directory with IRFinder outputs for experiment sample.}

\item{control_name}{Character prefix for naming control replicate columns.}

\item{experiment_name}{Character prefix for naming experiment replicate columns.}

\item{splice_min}{Numeric threshold (default = 10). Introns with minimum
SpliceExact < threshold across all replicates are removed.}
}
\value{
A data.frame containing:
\itemize{
\item \code{gene} as the first column
\item \code{index} and coordinate metadata
\item IRratio replicate columns for both control and experiment
\item Introns failing the SpliceExact filter removed
\item with chromosome, intron start, intron end information
}
}
\description{
Reads replicate IRFinder outputs from two directories (control and experiment),
merges by intron index, adds a \code{gene} column, filters introns based on
minimum SpliceExact across all replicates, and drops SpliceExact columns.
}
\examples{
\dontrun{
df <- read_irdir(
  control_dir     = "/path/HEK293T_SCRAMBLE",
  experiment_dir  = "/path/HEK293T_SNAR",
  control_name    = "HEK293T_scramble",
  experiment_name = "HEK293T_snaR",
  splice_min      = 10
)
head(df)
}
}
